# 一、存储类型
## 1.1 存储分类
```mermaid
graph TD
    A[存储分类] --> B[封闭存储系统]
    A --> C[开放存储系统]
    C --> D[内置存储]
    C --> E[外挂存储]
    E --> F[直连式存储 DAS]
    E --> G[网络存储 FAS]
    G --> H[存储区域网络 SAN]
    G --> I[网络附加存储 NAS]
```

> [!abstract] 开放存储系统
> 开放存储系统采用高速网络，把多套 PC 服务器连接起来，构建一个开放式系统，把这些 PC 作为存储节点，他提供可扩展的接口，方便软件安装以增强系统的功能
> 开放存储系统适用于需要大规模存储、高可扩展和灵活性的场景，如：云计算、大数据处理等领域

> [!abstract] 封闭存储系统
> 封闭存储系统主要指大型机或专用存储设备的存储架构，这些系统在设计时已经明确其用途和使用功能情景，不提供扩展接口来扩展软件功能
> 封闭存储系统适用于对性能和稳定性要求极高、且不需要频繁扩展存储容量的场景，如金融、电信等行业的关键业务系统。
## 1.2 存储类型

```mermaid
graph LR
    %% 设置样式
    classDef server fill:#c6e0ff,stroke:#000,stroke-width:1px
    classDef fs fill:#fdd8b3,stroke:#000,stroke-width:1px
    classDef switch fill:#d1e9d1,stroke:#000,stroke-width:1px
    classDef raid fill:#fdd8b3,stroke:#000,stroke-width:1px
    classDef jbod fill:#fdd8b3,stroke:#000,stroke-width:1px
    classDef disk fill:#cccccc,stroke:#000,stroke-width:1px,shape:cylinder
    classDef tag fill:#e3f2e3,stroke:#000,stroke-width:1px,stroke-dasharray:3 3
    
    %% DAS架构
    subgraph DAS [DAS - 直连式存储]
        direction TB
        DAS_Server[Application Server]:::server
        DAS_FS[File System]:::fs
        DAS_JBOD[JBOD]:::jbod
        DAS_Disk[Disk]:::disk
        DAS_Tag["DAS"]:::tag
        
        DAS_Server --> DAS_FS
        DAS_FS --> DAS_JBOD
        DAS_JBOD --> DAS_Disk
        DAS_Disk --> DAS_Tag
    end
    
    %% SAN架构
    subgraph SAN [SAN - 存储区域网络]
        direction TB
        SAN_Server[Application Server]:::server
        SAN_FS[File System]:::fs
        SAN_Switch[FC Switch]:::switch
        SAN_RAID[RAID]:::raid
        SAN_Disk[Disk]:::disk
        SAN_Tag["SAN"]:::tag
        
        SAN_Server --> SAN_FS
        SAN_FS --> SAN_Switch
        SAN_Switch --> SAN_RAID
        SAN_RAID --> SAN_Disk
        SAN_Disk --> SAN_Tag
    end
    
    %% NAS架构
    subgraph NAS [NAS - 网络附加存储]
        direction TB
        NAS_Server[Application Server]:::server
        NAS_Switch[Ethernet Switch]:::switch
        NAS_FS[File System]:::fs
        NAS_RAID[RAID]:::raid
        NAS_Disk1[Disk]:::disk
        NAS_Disk2[Disk]:::disk
        NAS_Tag["NAS"]:::tag
        
        NAS_Server --> NAS_Switch
        NAS_Switch --> NAS_FS
        NAS_Switch --> NAS_RAID
        NAS_FS --> NAS_Disk1
        NAS_RAID --> NAS_Disk2
        NAS_Disk1 --> NAS_Tag
        NAS_Disk2 --> NAS_Tag
    end
    
    %% 连接三个架构，确保它们横向排列
    DAS --> SAN
    SAN --> NAS
    linkStyle 7,8 stroke:#fff,stroke-width:0

```
### 1.2.1 DAS
直连式存储：Direct Attached Storage
> [!NOTE] 直连式存储
> 可以理解为本地文件系统。这种设备直接连接到计算机主板总线上，通常是 SCSI 或 FC 连接，计算机将其识别为一个块设备，例如常见的硬盘，U盘等，也可以连到一个磁盘阵列柜，里面有很多块磁盘。 DAS 只能连接一台服务器，其它服务器无法共享该存储。
### 1.2.2 SAN
存储区域网络：Storage Area Network
>[!note] 存储区域网络
>存储区域网络，这个是通过光纤通道或以太网交换机连接存储阵列和服务器主机，最后成为一个专用的存储网络。SAN 经过十多年历史的发展，已经相当成熟，成为业界的事实标准（但各个厂商的光纤交换技术不完全相同，其服务器和 SAN 存储有兼容性的要求）。
>




